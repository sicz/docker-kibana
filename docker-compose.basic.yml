###
### Kibana with X-Pack Basic configuration
###

version: "3.3"

services:
  kibana:
    environment:
      - XPACK_EDITION=${XPACK_EDITION}

  elasticsearch:
    environment:
      - XPACK_EDITION=${XPACK_EDITION}
